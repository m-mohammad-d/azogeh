import MetaTags from "../../components/MetaTag";
import PaymentForm from "../../components/PaymentForm";

function PaymentPage() {
  <MetaTags title="پرداخت | پنل کاربری" description="صفحه پرداخت سفارش‌ها." />;

  return (
    <div>
      <PaymentForm />
    </div>
  );
}

export default PaymentPage;
